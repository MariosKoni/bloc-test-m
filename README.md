# bloc_test_m

A minimal project in order to showcase the error.

## Reproduce the error

In order to reproduce the error shown in the thread, you need to launch the app without internet connection so that you trigger the error state in the bloc.